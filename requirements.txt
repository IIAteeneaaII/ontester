# ONT/ATA Testing Suite - Requirements
# Python: 3.11+ required

# Core dependencies for HTTP/HTTPS requests and HTML parsing
requests>=2.32.0
beautifulsoup4>=4.14.0

# Browser automation for ONT web login (Fiberhome devices)
selenium==4.38.0         # Web browser automation
webdriver-manager==4.0.2 # Automatic ChromeDriver management

# Network/Serial communication
paramiko>=4.0.0          # SSH connections
telnetlib3>=2.0.8        # Telnet protocol
pyserial>=3.5            # Serial port communication

# Cryptography (for future WiFi password decryption if AES key is found)
pycryptodome>=3.23.0     # AES encryption/decryption utilities

# Additional dependencies
urllib3>=2.5.0           # HTTP client (included with requests)